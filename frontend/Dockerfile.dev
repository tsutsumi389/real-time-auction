# ============================================
# Development Dockerfile with Hot Reload
# ============================================

FROM node:20-alpine

WORKDIR /app

# パッケージマネージャーファイルをコピー
COPY package*.json ./

# 依存関係のインストール
RUN npm install

# ソースコードはボリュームマウントで提供されるため、ここではコピーしない

# Vite開発サーバーを起動
CMD ["npm", "run", "dev"]
